# 여러 명이 협업할 때는 다음과 같으니 처리한다.
1. 새로운 멤버 합류 후 코드 내용 받기
    git clone git서버주소(url) .
    . : 이 안 붙으면 git 폴드 전체를 가져와서 현재 폴드의 하위 경로에 만들어 진다.
2. 내 작업 올릴 때 :  git add ==> git commit ==> git push
3. 남의 작업 가져올 때 : git pull  (작업 시작 전 습관적으로 실행할 내용..)
